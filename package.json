{
  "name": "numerical-card-battle",
  "version": "2.0.0",
  "private": true,
  "description": "数值卡牌 · 自动 PK — 选两张卡，调等级，开始战斗，看谁赢。",
  "type": "commonjs",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "verify": "node scripts/static-check.js && node --test tests/*.test.js && node scripts/acceptance.js",
    "serve": "node scripts/serve.js"
  }
}
